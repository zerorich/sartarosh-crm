import { PaymentDetailPage } from "@/views/payments/PaymentDetailPage";

export const metadata = {
  title: "Payment Transaction Details | CutZone Admin",
};

export default function Page() {
  return <PaymentDetailPage />;
}
