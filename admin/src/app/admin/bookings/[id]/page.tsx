import { BookingDetailPage } from "@/views/bookings/BookingDetailPage";

export const metadata = {
  title: "Booking Details | CutZone Admin",
};

export default function Page() {
  return <BookingDetailPage />;
}
